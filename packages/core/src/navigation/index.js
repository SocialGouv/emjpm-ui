import React from 'react';
import {Box} from 'rebass';

const baseStyle = {
  fontWeight: 700,
  color: 'black',
};

export const Navigation = (props) => {
  return (
    <Box>
      <Text>Title</Text>
    </Box>
  )
};